---
description: Dispatch the scribe agent to write docs, docstrings, or a commit message
argument-hint: <what to document>
allowed-tools: Agent
---

Dispatch the `scribe` agent with this documentation task: $ARGUMENTS

Pass it enough context to work cold — it does not share your conversation history.
Relay what it wrote and anything it flagged as unverifiable.
