---
description: Dispatch the debugger agent to root-cause a failure
argument-hint: <the failure, test name, or error>
allowed-tools: Agent
---

Dispatch the `debugger` agent to root-cause: $ARGUMENTS

Include the exact command that reproduces the failure and the full error output if
you have it — the agent does not share your conversation history.
Unless I asked for a fix, tell it to diagnose only. Relay its root cause and evidence.
