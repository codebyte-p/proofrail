---
description: Dispatch the tester agent to write and run tests
argument-hint: <what to test>
allowed-tools: Agent
---

Dispatch the `tester` agent with this testing task: $ARGUMENTS

Pass it enough context to work cold — it does not share your conversation history.
Relay the real pass/fail output it reports. Do not describe the suite as passing
unless it reported an actual run.
