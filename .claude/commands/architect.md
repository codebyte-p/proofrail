---
description: Dispatch the architect agent to design an implementation plan
argument-hint: <what to design>
allowed-tools: Agent
---

Dispatch the `architect` agent with this design task: $ARGUMENTS

Pass it enough context to work cold — it does not share your conversation history.
Relay the plan it returns. Do not start implementing it unless I say so.
